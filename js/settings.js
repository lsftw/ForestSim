
// Pause game when the browser tab isn't active / window isn't active
var PAUSE_ON_FOCUS_LOSS = true;

// Time between game frames, affects fps
var RUN_INTERVAL_MILLISECONDS = 50;
var FRAME_INTERVAL_MILLISECONDS = RUN_INTERVAL_MILLISECONDS;

// should be unused, affects color used to clear screen, only visible on undrawn regions
var BACKGROUND_COLOR = '#55A85B';

// toggles console logging
var LOG_DEBUG = true;


